const validar = (dados) => {
    if(!dados.nome == dados && dados.nome.length > 0 && dados ==""){
return nome.dados = false
    }
    else{
        nome.dados = True
    }
}

module.exports = {
    validar
}